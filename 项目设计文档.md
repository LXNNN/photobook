# 电子相册网站 - 项目设计文档

## 一、项目概述

一个纯前端的电子相册 H5 网站，用户可以上传照片，选择模板排版，生成一本书样式的电子相册，支持翻页预览和导出 PDF 打印。

### 核心功能
- 多本相册管理（首页书架展示）
- 照片上传（支持 JPG/PNG/HEIC，自动转换格式）
- 模板系统（多种排版模板，用户可切换）
- 文字编辑（标题、正文、自动换行）
- 图片裁剪/替换/拖拽互换位置
- 翻书预览（StPageFlip 翻页动画）
- 后续：PDF 导出，交付打印社打印

---

## 二、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18 | 前端框架 |
| TypeScript | 5 | 类型检查 |
| Vite | 5 | 构建工具 |
| Redux Toolkit | - | 状态管理 |
| Tailwind CSS | - | 样式 |
| StPageFlip (page-flip) | - | 翻书动画效果 |
| localforage | - | 本地数据存储（IndexedDB） |

### 技术选型理由
- **React**：用户熟悉，生态成熟，适合组件化开发
- **Redux Toolkit**：方便后续扩展后台，状态管理标准化
- **纯前端优先**：第一版用纯前端，用户数据存在本地浏览器；后续加后台部署，架构已预留扩展空间
- **A5 竖版开本**：适合打印，打印社标准尺寸

---

## 三、项目结构

```
src/
├── pages/
│   ├── ProjectList.tsx      # 首页 - 书架列表
│   ├── Editor.tsx           # 编辑页面
│   └── Viewer.tsx          # 预览页面 - 翻书效果
├── templates/
│   ├── index.ts             # 所有模板定义
│   └── copywriting.ts       # 通用文案库（常量）
├── engine/
│   ├── CanvasRenderer.ts    # Canvas 渲染引擎，文字自动换行
│   └── InteractionManager.ts # 画布交互管理（拖拽互换）
├── store/
│   └── projectSlice.ts       # Redux slice，相册数据管理
├── utils/
│   ├── autoLayout.ts         # 自动排版算法
│   ├── image.ts              # 图片处理工具（裁剪、压缩）
│   ├── storage.ts            # 本地存储
│   └── units.ts              # 单位换算（mm/px）
└── types/
    └── index.ts              # 类型定义
```

---

## 四、模板系统

### 模板分类
1. **封面尾页** - 杂志风封面、极简封面、尾页
2. **图片为主** - 单图满版、双图并列、三图错落
3. **图文混排** - 上图下文、左右图文、双图竖版
4. **多图拼贴** - 四宫格、九宫格、三图错落
5. **文字为主** - 文字封面

### 模板设计规范
- 相对坐标（0-1），自动适配不同尺寸
- 每个模板包含多个 element：image / text
- 图片自动裁剪（cover），不变形
- 文字自动换行，不超出边界

### 当前模板列表
| 模板 ID | 名称 | 分类 | 说明 |
|---------|------|------|------|
| cover-magazine | 杂志风封面 | 封面尾页 | 大图+文字叠加 |
| cover-magazine-v2 | 杂志风封面2 | 封面尾页 | 左侧竖排英文+右侧大标题+下方大图 |
| cover-minimal | 极简封面 | 封面尾页 | 左上角大标题，简洁高级 |
| back-cover | 尾页 | 封面尾页 | 封底 |
| full-image | 单图满版 | 图片为主 | 一张大图铺满全页 |
| full-image-bordered | 满版图（带边框） | 图片为主 | 一张图填满页面，四周留白边框 |
| dual-portrait | 双图竖版 | 图文混排 | 两张竖版照片并列，上下加标题和文案 |
| two-image-left | 左大图右小图 | 图文混排 | 左大右小，层次感 |
| three-collage-v2 | 三图错落 | 多图拼贴 | 左上大图+右上文字小图+底部横图 |
| ... | ... | ... | ... |

---

## 五、数据结构

### Project（相册）
```typescript
interface Project {
  id: string;
  name: string;
  coverImage: string;
  pages: Page[];
  createdAt: number;
  updatedAt: number;
}
```

### Page（页面）
```typescript
interface Page {
  id: string;
  templateId: string;
  elements: Element[];
}
```

### Element（元素）
```typescript
interface Element {
  id: string;
  type: 'image' | 'text';
  x: number;  // 相对坐标 0-1
  y: number;
  width: number;
  height: number;
  // image 类型
  imageId?: string;
  cropHint?: 'cover' | 'contain';
  // text 类型
  placeholder?: string;
  fontSizeRatio?: number;
  textAlign?: 'left' | 'center' | 'right';
  fontWeight?: 'normal' | 'bold';
  color?: string;
}
```

---

## 六、核心功能说明

### 1. 自动排版
用户一次上传多张图片，系统自动生成相册：
- 自动生成封面（第一张图片）
- 自动生成尾页
- 每页根据图片数量选择合适的模板
- 自动填充文案（从文案库随机抽取）
- 年份自动取当前年份

### 2. 图片处理
- 上传时自动压缩（200 DPI，保证打印清晰度）
- 图片自动裁剪（cover），不变形
- 支持 HEIC 格式，自动转换为 JPG
- 点击哪个区域上传，图片自动填充到那个区域

### 3. 交互
- 缩略图拖拽排序（左侧面板）
- 图片拖拽互换位置（不破坏模板结构）
- 模板切换：多出的图片自动新增页面
- 文字双击编辑

### 4. 翻书预览
- StPageFlip 实现翻页动画
- 书脊折痕效果（每一页内侧渐变阴影）
- 键盘左右键翻页
- 鼠标点击翻页
- 首次加载等待所有图片加载完成再显示，避免闪烁

---

## 七、后续规划

### V2 版本（加后台）
- 用户系统（登录/注册）
- 云端存储（替代本地 IndexedDB）
- 多设备同步
- 分享功能（生成链接分享相册）
- PDF 导出功能（打印社打印）

### V3 版本
- 更多模板（季节风格、节日风格）
- 背景音乐
- 视频相册
- 协作编辑

---

## 八、开本尺寸

| 尺寸 | 大小 | 适用场景 |
|------|------|----------|
| A5 竖版 | 148mm × 210mm | 当前使用，适合打印 |
| A4 | 210mm × 297mm | 更大尺寸，后续可选 |
| 20×20 | 200mm × 200mm | 正方形，后续可选 |

---

## 九、项目启动

```bash
# 安装依赖
npm install

# 开发模式
npx vite --port 5173

# 构建生产版本
npm run build
```

访问地址：http://localhost:5173/
